% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pbf01seq.R
\name{plot.bfseqdesign}
\alias{plot.bfseqdesign}
\title{Plot method for class \code{"bfseqdesign"}}
\usage{
\method{plot}{bfseqdesign}(
  x,
  plot = TRUE,
  nullplot = TRUE,
  zplot = FALSE,
  digits = max(3L, getOption("digits") - 3L),
  ...
)
}
\arguments{
\item{x}{Object of class \code{"power.bftest"}}

\item{plot}{Logical indicating whether data should be plotted. If
\code{FALSE} only the data used for plotting are returned.}

\item{nullplot}{Logcal indicating whether a second plot with the stopping
probabilities computed under the null hypothesis should also be produced.
Defaults to \code{TRUE}}

\item{zplot}{Logcal indicating whether a plot of the critical z-values should
be produced}

\item{digits}{Number of digits for formatting of numbers}

\item{...}{Other arguments (for consistency with the generic)}
}
\value{
Plots stopping curves (if specified) and invisibly returns a list of
    data frames containing the data underlying the stopping curves
}
\description{
Plot method for class \code{"bfseqdesign"}
}
\examples{
n <- seq(25, 150, 25) # sample size per stage
se <- sqrt(2/n) # standard errors per stage
res <- pbf01seq(k1 = 1/10, k0 = 3, se = se, n = n, pm = 0, psd = 1,
                dpm = 0.5, dpsd = 0.1, type = "moment")
plot(res, nullplot = FALSE) # only under design prior
plot(res, nullplot = TRUE) # also plot under null hypothesis
plot(res, zplot = TRUE) # show critical z-values

}
\seealso{
\link{pbf01seq}
}
\author{
Samuel Pawel
}
